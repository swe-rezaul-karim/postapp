apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: rezaul1998/post-backend:latest
          ports:
            - containerPort: 4040
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret